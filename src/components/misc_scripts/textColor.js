// Button text color
export const text_dark = {
    color: "black"
}
export const text_light = {
    color: "white"
}

// Import these then add `style={brightnessCatagory(color) === "bright" ? text_dark : text_light}` to any tag 